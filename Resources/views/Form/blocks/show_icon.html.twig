{% if icon is empty %}
        <img src="{{link}}" data-key="{{secretKey}}" />
{% else %}
        <img src="{{link}}" data-key="{{secretKey}}" /><div id="preview-template" style="display: none;"></div>
{% endif %}
